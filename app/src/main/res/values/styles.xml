<resources>

    <style name="AppTheme" />

    <style name="AppTheme.LightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryLight</item>
        <item name="colorPrimaryDark">@color/primaryDarkLight</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorAccent</item>

        <!-- override attr -->
        <item name="backgroundBody">@color/primaryLight</item>
        <item name="textColorPrimary">@color/primaryTextColorLight</item>
        <item name="textColorSecondary">@color/secondTextColorLight</item>
        <item name="itemBackground">@color/secondaryTextColor</item>
        <item name="bodyBackgroundSecond">@color/bodyBackgroundSecondLight</item>

        <item name="android:overScrollMode">never</item>
        <item name="android:windowBackground">@color/primaryLight</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.DarkTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primaryDark</item>
        <item name="colorPrimaryDark">@color/PrimaryDarkDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorAccent</item>

        <!-- override attr -->
        <item name="backgroundBody">@color/primaryDark</item>
        <item name="textColorPrimary">@color/primaryTextColorDark</item>
        <item name="textColorSecondary">@color/secondTextColorDark</item>
        <item name="itemBackground">@color/PrimaryDarkDark</item>
        <item name="bodyBackgroundSecond">@color/bodyBackgroundSecondDark</item>

        <item name="android:overScrollMode">never</item>
        <item name="android:windowBackground">@color/primaryDark</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Toolbar" />

    <style name="AppTheme.Toolbar.Default">
        <item name="android:textSize">@dimen/textLarge</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="ThemeOverlay.AppCompat.navTheme">
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorControlHighlight">@android:color/transparent</item>
    </style>

</resources>
